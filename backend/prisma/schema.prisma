generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Inspector {
  id        Int      @id @default(autoincrement())
  vehicleId Int
  date      DateTime
}
